<!-- 改编自：研究装修艺术主题项目提示词/ld-421/ld-421.md；后端由 NestJS+TypeScript 改造为 Go 1.22 + Gin + GORM -->

## 项目主要需求

### 项目概述

【全栈Web应用】实验室设备管理系统。面向高校和科研院所的实验室，支持设备资产登记、借用归还、维护保养、使用预约和设备状态看板。**本项目用于验证模型跨文件协同改动能力，禁止将多个职责合并进单一文件。**

### 核心实体（≥ 3 个，每个贯穿全栈）

1. **设备（Equipment）** — 字段：设备名称、设备编号、分类ID、品牌型号、序列号、购买日期、购买价格、存放位置（楼栋-房间-柜号）、资产状态（Available/InUse/Maintenance/Retired/Lost）、责任人ID、供应商、保修到期日、设备图片URL
   - 关联：1台设备 → 多条借用记录、多条维护记录、多条预约记录
   - 业务动作：登记入库、报废、转移责任人
   - **贯穿全栈**

2. **借用记录（BorrowRecord）** — 字段：设备ID、借用人ID、借用日期、预计归还日期、实际归还日期、借用事由、审批状态（Pending/Approved/Rejected/Returned/Overdue）、审批人ID、归还状况（Good/Damaged/Lost）
   - 业务动作：提交借用、审批、确认归还
   - **贯穿全栈**

3. **维护记录（MaintenanceRecord）** — 字段：设备ID、维护类型（Preventive/Corrective/Calibration/Cleaning）、维护内容、维护日期、下次维护日期、维护费用、维护人ID、维护结果（Pass/Fail/NeedsFollowUp）
   - 业务动作：创建维护计划、执行维护、记录结果
   - **贯穿全栈**

4. **预约记录（Reservation）** — 字段：设备ID、预约人ID、预约开始时间、预约结束时间、使用目的、审批状态（Pending/Approved/Rejected/Cancelled）、审批人ID
   - 业务动作：创建预约、审批预约、取消预约
   - **贯穿全栈**

5. **设备分类（EquipmentCategory）** — 字段：分类名称、父分类ID、描述、图标
   - **贯穿全栈**

### 核心页面（≥ 4 个）

1. **设备总览仪表盘** `/dashboard` — 消费：Equipment + BorrowRecord + MaintenanceRecord
   - 设备状态分布（饼图）、本月借用次数TOP10、即将过保设备列表、待审批借用/预约数量
   - 使用 `<StatusBadge>`, `<StatCard>`, `<AlertBanner>`

2. **设备管理** `/equipment` — 消费：Equipment + EquipmentCategory + BorrowRecord
   - 设备列表（树形分类+搜索），设备详情（基本信息+借用历史+维护记录+预约日历），新增/编辑设备
   - 使用 `<StatusBadge>`, `<CalendarCell>`

3. **借用管理** `/borrow` — 消费：BorrowRecord + Equipment
   - 借用申请列表（按状态Tab），提交借用（选设备+填信息），审批借用，确认归还
   - 使用 `<StatusBadge>`, `<StepIndicator>`

4. **预约管理** `/reservations` — 消费：Reservation + Equipment
   - 设备预约日历视图（按设备切换），创建预约（选时间段），审批预约
   - 使用 `<CalendarCell>`, `<StatusBadge>`

5. **维护管理** `/maintenance` — 消费：MaintenanceRecord + Equipment
   - 维护计划日历（即将到期高亮），执行维护记录，维护费用统计
   - 使用 `<StatusBadge>`, `<CalendarCell>`, `<StatCard>`

### 横切关注点（≥ 2 个）

1. **RBAC 权限控制**
   - 角色：Admin / LabManager / Researcher / Student
   - 触达文件层：DB role表 + RBAC中间件 + 路由守卫 + 按钮显隐（Student不能审批）+ 类型定义

2. **操作日志**
   - 记录设备登记、借用审批、归还确认、维护记录等关键操作
   - 触达文件层：DB audit_log表 + 日志中间件 + service埋点 + 前端日志页 + 类型定义

3. **全局异常处理**
   - 触达文件层：后端错误处理中间件 + handler业务异常 + 前端ErrorBoundary + API拦截器 + 错误码枚举

### 共享枚举/常量（≥ 2 组）

1. **资产状态 AssetStatus**：Available / InUse / Maintenance / Retired / Lost
   - 后端：`backend/internal/constants/enums.go`；前端：`frontend/src/types/enums.ts`

2. **借用审批状态 BorrowStatus**：Pending / Approved / Rejected / Returned / Overdue

3. **维护类型 MaintenanceType**：Preventive / Corrective / Calibration / Cleaning

4. **归还状况 ReturnCondition**：Good / Damaged / Lost

### 共享前端组件清单（≥ 3 个）

1. **`<StatusBadge>`** — 所有页面
2. **`<CalendarCell>`** — 设备详情、预约管理、维护管理
3. **`<StatCard>`** — 仪表盘、维护管理
4. **`<StepIndicator>`** — 借用管理（申请→审批→使用→归还）
5. **`<AlertBanner>`** — 仪表盘（过保预警、逾期借用）

### 共享 hooks/utils 清单（≥ 2 个）

1. **`useBorrowFlow`** — 封装借用全流程逻辑（申请→审批→使用→归还）
2. **`usePagination`** — 通用分页
3. **`formatCurrency`** — 金额格式化（设备价格、维护费用）

### 后端中间件清单（≥ 2 个）

1. **`AuthMiddleware`** — JWT 鉴权
2. **`RBACMiddleware`** — 角色校验
3. **`AuditLogMiddleware`** — 操作日志
4. **`ErrorHandlerMiddleware`** — 统一异常
5. **`RequestLoggerMiddleware`** — 请求日志

### 技术要求

- **英文短名（用于 Docker Compose）**：`lab-equipment`
- **前端端口**：18801 / 80
- **后端端口**：19301 / 8080
- **前端**：React 18 + TypeScript + Vite
- **UI 库**：Ant Design 5
- **图表**：ECharts
- **后端**：Go 1.22 + Gin + GORM
- **数据库**：MySQL 8.0
- **ORM**：GORM
- **认证**：JWT（使用 github.com/golang-jwt/jwt/v5）
- **状态管理**：Zustand
- **前端构建工具**：Vite
- **后端构建工具**：go build / go mod tidy

### 文件结构强制清单

**前端（严禁合并职责到单一文件）**：
```
frontend/src/
├── api/              # equipment.ts, borrow.ts, maintenance.ts, reservation.ts, category.ts
├── stores/           # equipmentStore.ts, borrowStore.ts, maintenanceStore.ts, reservationStore.ts
├── types/            # index.ts, enums.ts, equipment.ts, borrow.ts, maintenance.ts, reservation.ts
├── components/common/  # StatusBadge, CalendarCell, StatCard, StepIndicator, AlertBanner, EmptyState
├── hooks/            # useBorrowFlow.ts, usePagination.ts
├── pages/            # Dashboard, EquipmentManage, BorrowManage, ReservationManage, MaintenanceManage
├── router/           # index.tsx, guards.tsx
├── utils/            # formatCurrency.ts, request.ts
└── constants/        # apiPaths.ts
```

**后端（严禁合并职责到单一文件）**：
```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── internal/
│   ├── config/       # database_config.go, jwt_config.go
│   ├── model/        # equipment.go, borrow_record.go, maintenance_record.go, reservation.go, equipment_category.go
│   ├── repository/   # equipment_repository.go, borrow_repository.go, maintenance_repository.go, reservation_repository.go, category_repository.go
│   ├── service/      # equipment_service.go, borrow_service.go, maintenance_service.go, reservation_service.go
│   ├── handler/      # equipment_handler.go, borrow_handler.go, maintenance_handler.go, reservation_handler.go, category_handler.go
│   ├── router/       # equipment_router.go, borrow_router.go, maintenance_router.go, reservation_router.go, category_router.go
│   ├── middleware/   # auth.go, rbac.go, audit_log.go, error_handler.go, request_logger.go
│   ├── dto/          # equipment_dto.go, borrow_dto.go, maintenance_dto.go, reservation_dto.go
│   ├── constants/    # enums.go, error_codes.go
│   ├── errors/       # business_error.go, validation_error.go
│   └── logger/       # logger.go
└── database/
    ├── migrations/
    └── seeds/
```

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


### 部署要求

Docker Compose 一键部署。
- 顶层 `name: lab-equipment`
- 不写 `version:` 字段
- MySQL 命名卷 + healthcheck
- 后端 depends_on service_healthy
- 后端提供 `GET /healthz` 健康检查接口，并配置 healthcheck
- 前端 `${FRONTEND_PORT:-18801}:80`，后端 `${BACKEND_PORT:-19301}:8080`
- 前端 Nginx `location /api/` 反向代理到 `http://backend:8080/`
- 后端 `backend/Dockerfile` 采用 Go 多阶段构建（golang:1.22-alpine → alpine:3.20）
- `.env` 必含：`COMPOSE_PROJECT_NAME=lab-equipment`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_ROOT_PASSWORD`, `JWT_SECRET`, `FRONTEND_PORT`, `BACKEND_PORT`
- 支持 `docker compose up -d`（不需要 `-p`）

### backend/Dockerfile 参考（务必遵循）

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

### 构建验证清单

- `backend/` 下执行 `go mod tidy` 无错误
- `backend/` 下执行 `go build ./...` 无错误
- 在中文目录名下执行 `docker compose config --quiet` 不报错
- 启动后 `docker compose ps` 显示全部容器 healthy
- 浏览器访问 18801 端口可打开页面，19301 端口后端 API 返回正常

### Git 要求

`git init` + `.gitignore`（至少包含 `node_modules/`、`dist/`、`.env`、`.DS_Store`、`*.log`、`.idea/`、`.vscode/`，以及 Go 相关 `bin/`、`backend/server`、`*.exe`、`*.test`、`coverage.out`）。

### README 写法

中文 README，Docker 启动最前、访问地址（18801/19301）、技术栈表格（后端写 Go 1.22 + Gin + GORM）、目录结构、枚举位置、License。
